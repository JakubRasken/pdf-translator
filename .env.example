# Google Gemini API Key for translation (Required if not using --mock mode)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Default Gemini model (Defaults to gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash
